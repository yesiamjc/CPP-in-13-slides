#include <iostream>
using namespace std;
int main()
    {
      int a, b;
      cout<<"enter a :"<<endl;
      cin>>a;
      cout<<"enter b :"<<endl;
      cin>>b;
      cout<<"a + b = "<<a+b<<endl;
      cout<<"a - b = "<<a-b<<endl;
      cout<<"a * b = "<<a*b<<endl;
      cout<<"a / b = "<<(float)a/b<<endl;
      return 0;
    }
    /* For taking inputs code is cin>>the variable where to assign 
       arithmati operations are very similar to C and if some int 
       value may about to come answer in decimal simply use 
        cout<<"a / b = "<<(float)a/b<<endl; and go on 
        */