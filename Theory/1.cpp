#include <iostream> // The pre processing command of C++
using namespace std;
int main() // a function where the functions are stored which occurs firstly
/*{
    std :: cout<<"yesiamjc"<< std :: endl; // This will than be the line if we dont write the using namespace std;
    return 0;
}*/
{
    cout << "yesiamjc" << endl; // This is when we write the using namespace std; thats right small and classy
    return 0;                   //when the jobs done then return o
}

/*To run either press that play button on top right corner or type g++ .\filename.cpp 
in terminal next type .\a.exe and enter */